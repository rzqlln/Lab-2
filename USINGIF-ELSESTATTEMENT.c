/* AQUINO, RIZELLE FATE P. DIT1-2 */
#include <stdio.h>

int main() {
    int num = 37;

    if (num % 2 == 0) {
        printf("%d is even.\n", num);
    } else {
        printf("%d is odd.\n", num);
    }

    return 0;
}
